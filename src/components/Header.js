import React from "react";

import { Menu } from "antd";

const Header = () => {
  return (
    <Menu mode="horizontal">
      <Menu.Item key="mail">Calendar App</Menu.Item>
    </Menu>
  );
};

export default Header;
